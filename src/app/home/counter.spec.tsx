import { render, screen, fireEvent } from '@testing-library/react';import Counter from './Counter';describe('Counter Component', () => {  beforeEach(() => {    render(<Counter />);  });  it('should render the counter component', () => {    const counterText = screen.getByText(/counter:/i);    expect(counterText).toBeDefined();  });  describe('Increment functionality', () => {    it('should increment counter value when increment button is clicked', () => {      fireEvent.click(screen.getByText(/increment/i));      const incrementedValue = screen.getByText(/counter: 1/i);      expect(incrementedValue).toBeDefined();    });  });  describe('Decrement functionality', () => {    it('should decrement counter value when decrement button is clicked', () => {      fireEvent.click(screen.getByText(/decrement/i));      const decrementedValue = screen.getByText(/counter: -1/i);      expect(decrementedValue).toBeDefined();    });  });});