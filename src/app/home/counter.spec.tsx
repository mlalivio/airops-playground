import { render, screen, fireEvent } from '@testing-library/react';import Counter from './Counter';describe('Counter Component', () => {    beforeEach(() => {        render(<Counter />);    });    it('should display the counter component', () => {        const counterElement = screen.queryByText(/counter:/i);        expect(counterElement).toBeDefined();    });    it('should increment counter value on increment button click', () => {        const incrementButton = screen.getByText(/increment/i);        fireEvent.click(incrementButton);        const counterValue = screen.getByText(/counter: 1/i);        expect(counterValue).toBeDefined();    });    it('should decrement counter value on decrement button click', () => {        const decrementButton = screen.getByText(/decrement/i);        fireEvent.click(decrementButton);        const counterValue = screen.getByText(/counter: -1/i);        expect(counterValue).toBeDefined();    });});